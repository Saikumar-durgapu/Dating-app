import SwiftUI

struct PhotoUploadView: View {
    var body: some View {
        VStack(spacing: 14) {
            Text("Add up to 6 photos")
            Text("Photo picker integration placeholder for MVP")
                .foregroundStyle(.secondary)
        }
        .navigationTitle("Photos")
    }
}
